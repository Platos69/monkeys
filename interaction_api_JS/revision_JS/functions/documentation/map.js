// Realiza códigos em arrays, onde realiza esse feito um por um dos arrays, onde para demonstrar o resultado basta chamar a variavel criada para o mesmo

const array = [21, 6, 84, 24, 86, 11, 39, 59, 96, 53]

const multi = array.map((x) => x * 2)

console.log(multi)
